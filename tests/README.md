# tests

Automated verification layer for `app.js` logic. Empty for now — no test
runner is adopted yet (Node isn't installed on this machine as of
2026-07-29). Populate once a runner is chosen; a single documented command
to run all tests must exist before this folder is considered active.
