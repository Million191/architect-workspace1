{
  "name": "meeting-assistant-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Meeting Assistant backend: audio ingestion, transcription, minutes, and action-item tracking.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc --noEmit false -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "dev": "ts-node src/server.ts",
    "start": "node dist/server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.9",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.1.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
